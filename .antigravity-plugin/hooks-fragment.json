{
  "_note": "Antigravity loads hooks.json from the workspace's .agents/ directory (or ~/.gemini/config/). This file is the reference shape; `godmode hooks wire --host antigravity` merges the godmode key into the project's .agents/hooks.json with ${godmodePluginRoot} replaced by the absolute install path. stdout for PreToolUse must be a single JSON object {decision, reason}. Never claim HARD interception without a fresh probe proof; Stop hooks not firing on Windows (IDE 1.107.0) is field-confirmed, 2026-08-29.",
  "godmode": {
    "enabled": true,
    "PreToolUse": [
      {
        "matcher": ".*",
        "type": "command",
        "command": "python ${godmodePluginRoot}/hooks/godmode_gate_fast.py",
        "timeout": 8
      }
    ],
    "Stop": [
      {
        "type": "command",
        "command": "python ${godmodePluginRoot}/hooks/godmode_session_hook.py stop",
        "timeout": 10
      }
    ]
  }
}
